#!/usr/bin/env bash
# Convenience wrapper to launch Claude with permissions bypassed.
# This is intentionally a local helper; keep it in .gitignore or ignore it.
claude --permission-mode bypassPermissions
