apiVersion: v1
kind: Service
metadata:
  name: backend-v2
  namespace: llm-wiki
spec:
  type: NodePort
  selector:
    app: backend-v2
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30081
    name: http
